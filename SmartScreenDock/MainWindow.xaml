<Window x:Class="SmartScreenDock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmartScreenDock" 
        SizeToContent="WidthAndHeight" MinWidth="150"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Loaded="Window_Loaded" Left="0" Top="-2000">
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#E3E3E3"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkContextMenu" TargetType="ContextMenu">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" Padding="2">
                            <ItemsPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkMenuItem" TargetType="MenuItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Bd" Background="Transparent" Padding="15,8" CornerRadius="3">
                            <ContentPresenter ContentSource="Header" TextElement.Foreground="White"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#444444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#F21C1C1C" CornerRadius="12" BorderBrush="#33FFFFFF" BorderThickness="1" Margin="5">
        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,0">
            <WrapPanel x:Name="Block1_Utils" Orientation="Horizontal">
                <Button Content="&#xE721;" ToolTip="Поиск в Google (буфер)" Click="BtnSearch_Click"/>
                <Button Content="&#xF406;" ToolTip="Выделение области" Click="BtnScreenshotRegion_Click"/>
                <Button Content="&#xE714;" ToolTip="Запись видео" Click="BtnRecordVideo_Click"/>
                <Button Content="&#xF03E;" ToolTip="Буфер обмена" Click="BtnClipboard_Click"/>
                <Button Content="&#xE8EF;" ToolTip="Калькулятор" Click="BtnCalc_Click"/>
            </WrapPanel>

            <Rectangle x:Name="Sep1" Width="1" Height="28" Fill="#33FFFFFF" Margin="6,0" Visibility="Collapsed"/>
            <WrapPanel x:Name="Block2_AI" Orientation="Horizontal"/>
            
            <Rectangle x:Name="Sep2" Width="1" Height="28" Fill="#33FFFFFF" Margin="6,0" Visibility="Collapsed"/>
            <WrapPanel x:Name="Block3_Web" Orientation="Horizontal"/>
            
            <Rectangle x:Name="Sep3" Width="1" Height="28" Fill="#33FFFFFF" Margin="6,0" Visibility="Collapsed"/>
            <WrapPanel x:Name="Block4_Scripts" Orientation="Horizontal"/>

            <Rectangle x:Name="Sep4" Width="1" Height="28" Fill="#33FFFFFF" Margin="6,0" Visibility="Collapsed"/>
            <WrapPanel x:Name="Block5_Other" Orientation="Horizontal"/>

            <Rectangle Width="1" Height="28" Fill="#33FFFFFF" Margin="6,0"/>

            <Button Content="&#xE710;" ToolTip="Настройки / Добавить" Click="BtnSettings_Click" Foreground="#FFD700"/>
            <Button Content="&#xE711;" ToolTip="Закрыть" Click="BtnClose_Click" Foreground="#E3544C"/>
        </WrapPanel>
    </Border>
</Window>