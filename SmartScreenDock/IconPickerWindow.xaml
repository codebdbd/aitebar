<local:DarkWindow x:Class="SmartScreenDock.IconPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SmartScreenDock"
        Icon="pack://application:,,,/Resources/app.ico"
        Title="Каталог иконок" Height="580" Width="560"
        WindowStartupLocation="CenterScreen" Background="#1E1E1E" Foreground="White"
        WindowStyle="SingleBorderWindow" ResizeMode="NoResize" ShowInTaskbar="False"
        Topmost="True">

    <Window.Resources>
        <Style x:Key="IconBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#454545"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="#4285F4" BorderThickness="0,0,0,2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#454545"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Вкладки -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#252525">
            <Button x:Name="BtnTabMaterial" Content="Material Icons"
                    Margin="6,6,2,0"
                    Style="{StaticResource TabButtonStyle}"
                    Click="BtnTabMaterial_Click"/>
            <Button x:Name="BtnTabFluent" Content="Fluent System"
                    Margin="2,6,2,0"
                    Style="{StaticResource TabButtonStyle}"
                    Click="BtnTabFluent_Click"/>
            <Button x:Name="BtnTabBrands" Content="Brands"
                    Margin="2,6,6,0"
                    Style="{StaticResource TabButtonStyle}"
                    Click="BtnTabBrands_Click"/>
        </StackPanel>

        <!-- Строка поиска -->
        <Grid Grid.Row="1" Margin="10,8,10,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="🔍" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="#AAAAAA"/>
            <TextBox x:Name="TxtSearch" Grid.Column="1"
                     Background="#2D2D2D" Foreground="White"
                     BorderBrush="#444444" BorderThickness="1"
                     Height="30" Padding="8,0" VerticalContentAlignment="Center"
                     TextChanged="TxtSearch_TextChanged"/>
        </Grid>

        <TextBlock x:Name="TxtSearchHint" Grid.Row="2"
                   Margin="10,0,10,6" FontSize="11" Foreground="#666666"
                   Text="Поиск по коду, например: E000"/>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="10,0,10,10">
            <WrapPanel x:Name="IconPanel"/>
        </ScrollViewer>
    </Grid>
</local:DarkWindow>
