<local:DarkWindow x:Class="SmartScreenDock.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SmartScreenDock"
        Icon="pack://application:,,,/Resources/app.ico"
        Title="О программе"
        Width="540"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E"
        Foreground="White"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,16,0,6"/>
            <Setter Property="Foreground" Value="#F0F0F0"/>
        </Style>

        <Style x:Key="MutedTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#B8B8B8"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="Background" Value="#4285F4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#5A95F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InfoBoxStyle" TargetType="Border">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#7FB3FF"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,16,6"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock TextDecorations="Underline"
                                   Text="{TemplateBinding Content}"
                                   Foreground="{TemplateBinding Foreground}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="AiteBar" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBlock x:Name="TxtVersion" Style="{StaticResource MutedTextStyle}" Margin="0,0,0,14"/>

            <TextBlock Style="{StaticResource MutedTextStyle}"
                       Text="Скрытая панель быстрого доступа для Windows. Позволяет запускать сайты, приложения, скрипты, команды и горячие клавиши из настраиваемых блоков."/>

            <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="Информация"/>
            <TextBlock Style="{StaticResource MutedTextStyle}" Text="Разработчик: Codebdbd"/>
            <TextBlock Style="{StaticResource MutedTextStyle}" Text="© 2026 Codebdbd"/>

            <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="Технологии"/>
            <TextBlock Style="{StaticResource MutedTextStyle}" Text=".NET 8"/>
            <TextBlock Style="{StaticResource MutedTextStyle}" Text="WPF"/>

            <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="Данные приложения"/>
            <Border Style="{StaticResource InfoBoxStyle}">
                <TextBlock x:Name="TxtDataPath" Style="{StaticResource MutedTextStyle}"/>
            </Border>
            <Button Content="Открыть папку данных" Style="{StaticResource LinkButtonStyle}" Click="BtnOpenDataFolder_Click"/>

            <TextBlock Style="{StaticResource SectionHeaderStyle}" Text="Ресурсы"/>
            <WrapPanel>
                <Button Content="Страница программы" Style="{StaticResource LinkButtonStyle}" Click="BtnWebsite_Click"/>
                <Button Content="GitHub репозиторий" Style="{StaticResource LinkButtonStyle}" Click="BtnRepository_Click"/>
                <Button Content="Лицензии" Style="{StaticResource LinkButtonStyle}" Click="BtnLicenses_Click"/>
                <Button Content="Папка программы" Style="{StaticResource LinkButtonStyle}" Click="BtnOpenProgramFolder_Click"/>
            </WrapPanel>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Закрыть" Width="90" Style="{StaticResource PrimaryButtonStyle}" Margin="0" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</local:DarkWindow>

