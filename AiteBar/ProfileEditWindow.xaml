<local:DarkWindow x:Class="AiteBar.ProfileEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AiteBar"
        Icon="pack://application:,,,/Resources/app.ico"
        Title="Настройки профиля" Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" Background="#1E1E1E" Foreground="White"
        WindowStyle="SingleBorderWindow" ResizeMode="NoResize" ShowInTaskbar="False"
        Topmost="True">
    
    <Window.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4285F4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5C9AFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="Padding" Value="8,0,8,0"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <StackPanel Margin="20">
        <TextBlock Text="Название профиля:" Foreground="#AAA" Margin="0,0,0,5"/>
        <TextBox x:Name="TxtName" Margin="0,0,0,20"/>

        <TextBlock Text="Иконка профиля:" Foreground="#AAA" Margin="0,0,0,5"/>
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Width="50" Height="50" Background="#252525" CornerRadius="8" BorderBrush="#444444" BorderThickness="1">
                <Grid>
                    <TextBlock x:Name="PreviewIcon" Text="&#xF36A;" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FFD700"/>
                </Grid>
            </Border>
            <Button Grid.Column="1" Content="Выбрать иконку" Style="{StaticResource ActionButtonStyle}" Margin="15,0,0,0" Click="BtnPickIcon_Click"/>
        </Grid>

        <TextBlock Text="Цвет иконки:" Foreground="#AAA" Margin="0,0,0,5"/>
        <Grid x:Name="GridColors" Margin="0,0,0,20">
            <!-- Will be populated in code -->
            <UniformGrid x:Name="UniformColors" Columns="5"/>
        </Grid>

        <UniformGrid Columns="2" Margin="0,10,0,0">
            <Button Content="Отмена" Style="{StaticResource ActionButtonStyle}" Background="#444444" Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button Content="Сохранить" Style="{StaticResource ActionButtonStyle}" Click="BtnSave_Click"/>
        </UniformGrid>
    </StackPanel>
</local:DarkWindow>
